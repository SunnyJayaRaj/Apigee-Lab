openapi: 3.0.3

info:
  title: Secure Bank Access API
  description: |
    A sample banking API demonstrating OAuth 2.0 Client Credentials
    authentication using Google Cloud Apigee.

    This specification is intended for learning and portfolio purposes.
  version: 1.0.0
  contact:
    name: Sunny JayaRaju
    url: https://github.com/SunnyJayaRaju
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

servers:
  - url: https://api.bank-lab.com/v1
    description: Mock Backend
paths:
  # 1. The "Front Desk" - Where you get your Badge (Token)
  /token:
    post:
      summary: Generate Access Token
      description: Exchange Client ID and Secret for an OAuth Bearer Token.
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                grant_type:
                  type: string
                  example: client_credentials
                client_id:
                  type: string
                client_secret:
                  type: string
      responses:
        '200':
          description: Successful Token
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string
                    example: "ey...789"
                  expires_in:
                    type: integer
                    example: 300
  
  # 2. The "Vault" - Requires the Badge (Token) to enter
  /balance:
    get:
      summary: Get Account Balance
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Balance Retrieval
          content:
            application/json:
              schema:
                type: object
                properties:
                  account_id:
                    type: string
                    example: "123-456-789"
                  currency:
                    type: string
                    example: "USD"
                  amount:
                    type: number
                    example: 5400.50

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
